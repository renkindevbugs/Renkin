*.iml
.gradle
/local.properties
.idea
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties
app/release
.kotlin
# Local reference clone of Mihon (https://github.com/mihonapp/mihon), for UI inspiration only - never committed
mihon-reference/
# Local-only AI notes (release process, credential location) — never committed to the public repo
CLAUDE.local.md
# Local AI workflow and tool state — never committed to the public repo
/CLAUDE.md
/.claude/
# Local-only code-graph tooling (graphify / code-review-graph) + everything it generates —
# analysis helpers for the AI assistant, never part of the public repo
/.codegraph/
.graphify/
.crg/
graphify-out/
graph.html
graph.json
GRAPH_REPORT.md
*.graphml
code_graph.db
code_graph.sqlite
