*.iml
.gradle
/local.properties
.idea
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties
app/release
.kotlin
# Local reference clone of Mihon (https://github.com/mihonapp/mihon), for UI inspiration only - never committed
mihon-reference/
# Local-only AI notes (release process, credential location) — never committed to the public repo
CLAUDE.local.md
# Local AI workflow and tool state — never committed to the public repo
/CLAUDE.md
/.claude/
# Local-only AI assistant tooling — never committed to the public repo.
# codebase-memory-mcp binary + its per-agent MCP config; Codex's local AGENTS.md instructions.
/.aitools/
/.mcp.json
/AGENTS.md
